# Only needed in development for manually pushing/pulling a built container to ghcr.io registry.
# Used in `./tools/dev-k8s.sh`
DOCKER_USERNAME=vredchenko
DOCKER_EMAIL=val.redchenko@diamond.ac.uk
# DOCKER_PASSWORD is optional - script will auto-detect from 'gh auth token' if not set

# Optional env vars only used for optional MCP server
#SMARTEM_MCP_DATA_PATH=/path/to/epu/sessions    # Filesystem adapter
#SMARTEM_MCP_API_URL=http://localhost:8000        # API adapter
#SMARTEM_MCP_ADAPTER=filesystem                 # Adapter selection
#SMARTEM_MCP_LOG_LEVEL=INFO                     # Logging configuration
